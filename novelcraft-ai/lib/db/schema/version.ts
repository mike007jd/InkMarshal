/** Current on-disk SQLite schema epoch. Keep fixtures and migration gates on this source. */
export const CURRENT_SCHEMA_VERSION = 21;

export const CURRENT_SCHEMA_DESCRIPTION = `current_epoch_v${CURRENT_SCHEMA_VERSION}`;
